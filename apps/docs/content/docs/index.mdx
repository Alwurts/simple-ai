---
title: Introduction
description: Chat UI for shadcn. Copy it into your app.
---

simple-ai is a shadcn registry of chat UI. Copy the source into your app and own it.

The catalog is a chat input, a few pieces used in a real transcript (reasoning, tools, voice, shell), and one full chat page.

Flows, agents, and extra primitives from earlier simple-ai are gone. Use [shadcn](https://ui.shadcn.com) for messages and markdown.

## Install

```bash
npx shadcn@latest add @simple-ai/chat-input
npx shadcn@latest add @simple-ai/chat-page
```

Until `@simple-ai` is back in the official directory, point `components.json` at the hosted registry:

```json
{
  "registries": {
    "@simple-ai": "https://www.simple-ai.dev/r/{name}.json"
  }
}
```

Or install by URL:

```bash
npx shadcn@latest add https://www.simple-ai.dev/r/chat-input.json
```

GitHub works too, once this repo's root `registry.json` is on `main`:

```bash
npx shadcn@latest add Alwurts/simple-ai/chat-input
```
