export default function ChatApiNextPreview() {
  return (
    <p className="p-6 text-muted-foreground text-sm">
      Next.js App Router POST <code>/api/chat</code>. Add with{" "}
      <code>@simple-ai/chat-page</code>, then point useChat at{" "}
      <code>/api/chat</code>.
    </p>
  );
}
